# Versiones instaladas y probadas con Python 3.11 en Windows
# Última actualización: Mayo 2026

# Web scraping y extracción
newspaper3k==0.2.8
playwright==1.59.0
requests==2.31.0
lxml==6.1.0
lxml_html_clean>=0.4.0

# Base de datos
psycopg2-binary==2.9.12

# Variables de entorno
python-dotenv==1.0.1

# Servidor HTTP — expone /run para que api/ dispare el pipeline
# vía red interna de Docker (ver server.py)
flask==3.0.3
gunicorn==22.0.0

# NLP — embeddings y modelo de lenguaje
transformers>=4.40.0
# torch>=2.3.0  (se instala aparte en el Dockerfile, versión CPU-only)

# NLP — segmentación de oraciones en español (usado por annotator.py)
spacy>=3.7.0

# NLP — procesamiento lingüístico en español
nltk==3.8.1

# Clustering y machine learning
scikit-learn>=1.4.2
numpy>=1.26.4

# Clasificador (classifier.py) — XGBoost + análisis de importancia
xgboost>=2.0.0
shap>=0.45.0

# Análisis de sentimiento en español (usado por classifier.py)
pysentimiento>=0.7.0
